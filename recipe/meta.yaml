{% set name = "anchorboosting" %}
{% set version = "0.3.0" %}
{% set python_min = "3.9" %}
package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/anchorboosting-{{ version }}.tar.gz
  sha256: 797439b17b820177926c5cbf77a2a881a6967b3a57baac3fd5148867ada4ee5d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - lightgbm

test:
  imports:
    - anchorboosting
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: Efficient tree-based nonlinear anchor regression and classification for Python.
  home: https://github.com/mlondschien/anchorboosting
  doc_url: https://anchorboosting.readthedocs.io
  dev_url: https://github.com/mlondschien/anchorboosting
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mlondschien
